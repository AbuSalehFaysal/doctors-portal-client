import React from 'react';

const AppointmentByDate = ({appointments}) => {
    return (
        <div>
            <h1>Appointments {appointments.length}</h1>
        </div>
    );
};

export default AppointmentByDate;<h1>Appointments Date</h1>